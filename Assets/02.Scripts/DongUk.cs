using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DongUk : Human
{
    // Start is called before the first frame update
    void Start()
    {
        myName = "±èµ¿¿í";
        //myAge = 18;
        Debug.Log(myName);
        Eat();
    }
    

    // Update is called once per frame
    void Update()
    {
        
    }
}
